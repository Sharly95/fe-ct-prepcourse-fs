function divide(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de su división.
  // Tu código:
  return 'El resultado de la divicion es: ' + (x / y);
}

module.exports = divide(100, 4);