function resta(x, y) {
  // La función recibe dos argumentos "x" e "y" que son números.
  // Retorna el resultado de la resta.
  // Tu código:
  return 'El resultado de la resta es:' + (x - y);
}

module.exports = resta(100, 50);