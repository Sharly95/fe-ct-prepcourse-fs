function areaDelTriangulo(base, altura) {
  // Calcula el área de un triángulo y retorna el resultado.
  // Tu código:
  return 'El area de un triangulo es: ' + ((base * altura) / 2);
}

module.exports = areaDelTriangulo(3, 2);
